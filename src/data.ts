// Legacy component contracts retained while the former portal modules remain in the repository.
export type RoleId = "admin" | "faculty" | "student";
export type SubjectType = "Theory Only" | "Lab Only" | "Theory + Lab";
